services:
  demo:
    build: .
    network_mode: host
    cap_add: [NET_RAW, NET_ADMIN]
    env_file: .env
    volumes:
      - letsencrypt:/etc/letsencrypt
      - ./logs:/demo/logs
    stdin_open: true
    tty: true

volumes:
  letsencrypt:
