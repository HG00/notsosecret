DOMAIN=www.example.com
EMAIL=you@example.com
# Public-facing network interface on the HOST (not a container name).
# With --network=host the container sees the host's interfaces directly.
# Find yours: ip -4 addr show scope global
IFACE=eth0
